- clean: [
    '~',
]

- link:
    ~/.zshrc:
      force: true
      path: .zshrc
    ~/.p10k.zsh:
        force: true
        path: .p10k.zsh

- shell:
    - [git submodule update --init --recursive, Installing submodules]